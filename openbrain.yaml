openbrain-base-image: all
entry: python
args:
        - test.py
        - type: file
          io: input
          category: image
        - type: choice
          from-file: layer-list.txt
        - type: file
          io: output
          category: image
input: null
output: stdout
executes:
        - wget -nc https://storage.googleapis.com/download.tensorflow.org/models/inception5h.zip
        - unzip -n inception5h.zip
